import RewardPopup from '@/widgets/reward-popup/ui/reward-popup';
import { FC } from 'react';
interface IPopupsProps {}

const Popups: FC<IPopupsProps> = props => {
	const {} = props;

	return (
		<>
			<RewardPopup />
		</>
	);
};

export default Popups;
